#include "include/common.h"
#include "include/util.h"
#include "include/dbg.h"

#include <vector>

using namespace std;

template<typename T1, typename T2>
int minimize1(const vector<T1> residents, const vector<T2> distances, T2 &mailbox) {
  int err = 0;

  if(residents.size() & 1) {
    // middle point
  } else {

  }

  
  return err;
}

int main(int argc, char **argv) {

  return 0;
}
