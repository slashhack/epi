#include "include/common.h"
#include "include/util.h"
#include "include/dbg.h"

#include <string>

using namespace std;

int sinusoid(string in, string &out) {
  int err = 0;

  // build top level (pos 1 + 4k)

  // build second level (pos 2k)

  // build bottom level (pos 3 + 4k) or leftovers

  return err;
}

int main(int argc, char **argv) {

  return 0;
}
