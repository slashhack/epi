#include "include/common.h"
#include "include/util.h"
#include "include/dbg.h"

#include <string>
#include <vector>

using namespace std;

template <typename T>
int encode1(vector<T> in, string &out) {
  int err = 0;

  
  
  return err;
}

template <typename T>
int decode1(string in, vector<T> &out) {
  int err = 0;

  return err;
}
 
int main(int argc, char **argv) {

  return 0;
}
