#include "include/common.h"

template <typename T>
int reverse1(T in, T &out) {
  int err = 0;

  // string reverse
  
  return err;
}

template <typename T>
int reverse2(T in, T &out) {
  int err = 0;

  // solution from book
  
  return err;
}

int main(int argc, char **argv) {

  return 0;
}
