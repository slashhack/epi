#include "include/common.h"

template <typename T>
int palindrome1(T in, bool &palindrome) {
  int err = 0;

  // if a string
  
  return err;
}

template <typename T>
int palindrome2(T in, bool &palindrome) {
  int err = 0;

  T reverse = 0;

  bool is_pan = false;
  if(in == reverse) {
    is_pan = true;
  }
  palindrome = is_pan;
  
  return err;
}

// solution from book
  

int main(int argc, char **argv) {

  return 0;
}
