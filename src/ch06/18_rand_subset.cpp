#include "include/common.h"
#include "include/util.h"
#include "include/dbg.h"

#include <vector>

using namespace std;

int rand_subset1(int N, int k, vector<int> &subset) {
  int err = 0;

  // assume there's a funtion to select random k numbers in (0, ..., n
  // - 1)

  
  return err;
}

int main(int argc, char **argv) {

  return 0;
}
