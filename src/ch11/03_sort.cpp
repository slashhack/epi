#include "include/common.h"
#include "include/util.h"
#include "include/dbg.h"

#include <vector>

using namespace std;

template<typename T>
int sort1(const vector<T> in, const int k, vector<T> &out) {
  int err = 0;

  // use min heap with capacity k + 1
  // whenever heap is full, then pop min one store it
  // property is item is far at most k positions from original
  
  return err;
}
 
int main(int argc, char **argv) {

  return 0;
}
