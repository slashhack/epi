#include "include/common.h"
#include "include/util.h"
#include "include/dbg.h"

#include <vector>

using namespace std;

template<typename T>
int find_k(const vector<T> heap, const int k, T &out) {
  int err = 0;

  // need to think about heap's strucutre in a flat array

    
  return err;
}

int main(int argc, char **argv) {


  return 0;
}
