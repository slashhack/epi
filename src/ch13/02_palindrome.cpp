#include "include/common.h"
#include "include/util.h"
#include "include/dbg.h"

#include <string>

using namespace std;


// still can't get the meanning of "permutated from the panlidrome"
// what is the diffrerence between "permuated blah" and just
// permutation from the original string

int palindrome1(const string word, bool &palindrome) {
  int err = 0;

  // reverse and check the same with the original

  // matching from both beggining and at the end

  // more?
  
  return err;
}

int main(int argc, char **argv) {

  return 0;
}
